// Definir un CRUD

function CreateNote(note) {
    //
    const { title, description } = note

    



    console.log(title, description)
}

CreateNote({title: 'nota de prueba 0', description: 'text content that define my very first note on this'})